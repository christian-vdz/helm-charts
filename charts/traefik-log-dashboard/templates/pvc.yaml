apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "traefik-log-dashboard.fullname" . }}-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
