{{- if not  .Values.papra.useExistingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "app.fullname" . }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
type: Opaque
data:
  AUTH_SECRET: {{ .Values.papra.authSecret | b64enc | quote }}
{{- end }}
